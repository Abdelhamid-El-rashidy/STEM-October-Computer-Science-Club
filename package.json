{
  "name": "ocsc-website",
  "version": "1.0.0",
  "description": "Fira Code Variable Font",
  "main": "script.js",
  "scripts": {
    "start": "concurrently \"json-server --watch database/articles.json --port 3001\" \"json-server --watch database/projects.json --port 3002\"",
    "build": "npm-run-all build:*",
    "build:copy-files": "xcopy /s /e /y database dist\\database"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^8.2.0",
    "npm-run-all": "^4.1.2"
  },
  "engines": {
    "node": ">=6.11.3"
  },
  "dependencies": {
    "bootstrap": "^5.3.0",
    "json-server": "^0.17.1",
    "leaflet": "^1.9.4"
  }
}

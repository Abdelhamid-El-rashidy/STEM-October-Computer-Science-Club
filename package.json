{
  "dependencies": {
    "bootstrap": "^5.3.0",
    "json-server": "^0.17.1",
    "leaflet": "^1.9.4",
    "main": "script.js",
    "engines": {
      "node": "14.x"
    }
  },
  "name": "ocsc-website",
  "description": "Fira Code Variable Font",
  "version": "1.0.0",
  "main": "script.js",
  "scripts": {
    "start": "concurrently \"json-server --watch database/articles.json --port 3001\" \"json-server --watch database/projects.json --port 3002\"",
    "build": "npm-run-all build:*",
    "build:copy-files": "mkdir -p dist/database && cp -R database dist/database"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^8.2.0",
    "npm-run-all": "^4.1.2"
  }
}
